# ChroQueTas/FungAMR-derived AMR container

Container tag used by the workflow:

```text
gmboowa/rmap-myc-candida-amr:2026.05-chroquetas-v7-fixed
```

Reviewer access check:

```bash
docker pull gmboowa/rmap-myc-candida-amr:2026.05-chroquetas-v7-fixed
```

AMR outputs are screening evidence only. Absence of a curated marker is reported as absence of a configured genomic marker, not as phenotypic susceptibility.
